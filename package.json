{
  "name": "chrome-injection-extension",
  "version": "1.0.0",
  "description": "Chrome extension for injecting iframe overlays on specific websites",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.0.0"
  },
  "private": true
}